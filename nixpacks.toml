# Railway Nixpacks Configuration for AtlaSense
[start]
cmd = 'rm -f bootstrap/cache/*.php && php artisan config:clear && php artisan migrate --seed --force && php artisan serve --host 0.0.0.0 --port ${PORT:-8080}'
